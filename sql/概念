
当主键作为别人的外键时，基本上，leftjoin查出来的是一个collection。
如果自己含有外键引用其他人的主键，leftjoin查出来是一个association。

vchar(10) 10表示字符数量，根据底层的字符集来决定，并不是实际占用的字节数。
如果是utf8mb 占每个字符用4个字节，则占用大小是4倍

on 表示连接条件
where 表示过滤条件

修改表时，只能对表属性修改。要想修改约束，要从新用修改约束方式。
