popd pushd dirs    
    这三个命令是一起进行目录栈操作的.
        pushd [-n] [+N | -N | dir]
        popd [-n] [+N | -N]
    这两个分别对应入栈和出栈.它们的参数意义相同.
        -n选项表示不进行当前目录改动,仅仅改动栈中的目录数据
        [+N|-N]参数表示从左还是右进行.
            pushd +N 表示从左面第N个目录放到栈顶
            popd +N 表示把左面第N个目录移除栈
        仅出现在pushd中dir参数是添加dir到栈顶,如果不存在-n,则并且改变当前目录到它.
    dirs用来显示当前栈.可以增加-p选项按行显示,-c清空当前栈.

ar,建立, 修改档案或从档案中抽取成员
    ar -x  xxx 从归档文件中抽取文件

sed ':a ; N;s/\n/ / ; t a ; '
    合并文件中所有行
